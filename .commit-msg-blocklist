# Terms that must never appear in commit messages in this PUBLIC repo.
# One regex per line; lines starting with # are ignored.
# Patterns are evaluated case-insensitively as ERE (`grep -niE`).
# Use \b word boundaries on short tokens to avoid false positives
# (e.g. bare TARS would match "Stars" without boundaries).

# Client names (per global hygiene config)
dagne-dover
digital-trax
youmail
supply-clinic

# Personal infrastructure
\bTARS\b
\bskynet\b
\btailscale\b
\bPopoloto\b
\bRepos\.nosync\b

# Device IDs and hostnames
\bFVQFBFV\b
\bVRIGWPI\b

# Internal codenames / private repo references
\brrb-framer\b
\baouda-dev\b
\bclaude-marketing-dev\b

# Vague placeholder commits
^wip$
^fix$
^update$
^test$
